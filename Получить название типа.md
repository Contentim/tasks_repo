```
/*
	backend_mng@websoft.platform.bpm => 
	backend_mng_lib.js => 
	function **GetResultFieldsStatisticRec**
*/

oType = common.template_field_types.GetOptChildByKey(elem.type.Value);
type = oType.name.Value
```