#websoft #баги


пример - используем person_id чтобы отсечь запуск этапа курса. за пример взять 
если попытаться редактировать запрос по п
для того, чтобы отредактированный запрос
OptInt(curObject.person_id,null) == OptInt(1105387902724063510)