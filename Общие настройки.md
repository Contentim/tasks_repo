#websoft 

```
// включить Проверять заполнение требуемых полей
global_settings.settings.check_user_required_fields = 1;

// Язык веб интерфейса
global_settings.settings.required_fields.ObtainChildByKey('lng_id', 'name').is_web_edit = 1;

// сохранить настройки
global_settings.Doc.Save();

```